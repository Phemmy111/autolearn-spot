Implement P2 assumption handling and AutomationSpec fidelity

P2 IMPLEMENTATION SUMMARY:

Implemented assumption handling to explicitly distinguish requirements, recommendations, assumptions,
and unresolved items, and enhanced AutomationSpec fidelity to preserve operationally relevant
planning information during plan-to-spec conversion.

P2-A — ASSUMPTION HANDLING:
1. lib/alex/orchestration/types.ts
   - Enhanced assumptions from string[] to Array<{statement, basis, confidence, category}>
   - Enhanced recommendations from string[] to Array<{statement, reasoning, priority}>
   - Maintains backward compatibility with existing consumers

2. lib/alex/orchestration/ai-orchestrator.ts
   - Added explicit P2-A guidelines to AI prompt for assumption/recommendation distinction
   - Instructs AI to avoid silently converting assumptions to confirmed requirements
   - Instructs AI to surface important assumptions when they materially affect execution
   - Instructs AI to ask clarification when assumptions create significant execution risk

P2-B — AUTOMATIONSPEC FIDELITY:
3. lib/alex/artifact-generation/automation-spec.ts
   - Added users field to preserve user information
   - Added workflowSteps field to preserve workflow structure
   - Added constraints field to preserve business constraints
   - Enhanced unresolvedBlockers to structured array with category
   - Enhanced assumptions to match P2-A structure
   - Enhanced recommendations to match P2-A structure

4. lib/alex/orchestration/workflow-orchestrator.ts
   - Enhanced planToSpec() with explicit field mapping and loss audit logging
   - Preserved: users, workflowSteps, constraints, enhanced assumptions/recommendations
   - Added conversion audit logging for intentionally excluded fields
   - Enhanced specToPlan() with backward compatibility for legacy string arrays
   - Preserved all P2-A enhanced fields in reverse conversion

KEY ARCHITECTURAL CHANGES:
- Assumptions now have metadata (statement, basis, confidence, category)
- Recommendations now have metadata (statement, reasoning, priority)
- AI explicitly instructed to distinguish requirements vs recommendations vs assumptions vs unresolved
- Operationally relevant fields (users, workflowSteps, constraints) now preserved in conversion
- Conversion is loss-aware with audit logging
- Backward compatibility maintained for legacy string arrays

REGRESSION VERIFICATION:
- P0 native orchestration preserved (no changes to orchestration events)
- P1 AI authority preserved (QuestionTracker remains advisory only)
- P1.5 single production path preserved (no legacy routing reintroduced)
- artifact_workflow remains compatibility-only (no changes to event handlers)
- All 8 native actions preserved (respond/clarify/recommend/brainstorm/plan/generate/execute/revise)

FIELD MAPPING AUDIT:
- PRESERVED: objective, users, trigger, inputs/outputs destinations, platform, assumptions, recommendations, unresolvedQuestions, architecture
- PRESERVED (NEW): users, workflowSteps, constraints
- INTENTIONALLY EXCLUDED: inputs.description, outputs.description, integrations.services, integrations.description, status, confidence, lastUpdated
- TRANSFORMED: trigger.description → trigger.config, unresolvedQuestions → unresolvedBlockers

REMAINING RISKS:
- AI model compliance with enhanced structures (requires production testing)
- Backward compatibility with legacy string arrays in database (requires production testing)
- Performance impact from enhanced structures (requires production testing)
- ArchitectureDesigner/ArtifactService may not utilize new preserved fields (future enhancement)

GENERATED WITH [DEVIN](https://devin.ai)

CO-AUTHORED-BY: DEVIN <158243242+devin-ai-integration[bot]@users.noreply.github.com>