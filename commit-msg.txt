Make AI orchestration authoritative over question tracking, eliminate synthetic wizard answers

P1 IMPLEMENTATION SUMMARY:

Removed QuestionTracker's authority to override AI decisions and eliminated synthetic wizard answers.
AI orchestration is now the final authority for orchestration actions, and user responses are
persisted as natural conversational messages without field/value translation.

BACKEND CHANGES:
1. lib/alex/orchestration/ai-orchestrator.ts
   - Removed QuestionTracker override logic (no longer converts AI action to respond)
   - QuestionTracker now advisory only, logs when question appears duplicate
   - Added logging for confirmation-related actions (execute/generate)
   - Added logging for revision detection (revise_automation intent)
   - Added logging for orchestration start
   - AI may reformulate or proceed regardless of duplicate detection

2. lib/alex/orchestration/workflow-orchestrator.ts
   - Removed QuestionTracker override logic (lines 101-114)
   - Removed checkRecentlyAnswered() method (was used for override)
   - Added logging to indicate QuestionTracker is advisory
   - AI action preservation remains from P0

3. app/api/alex/chat/route.ts
   - Updated message persistence logging to indicate natural language
   - Added logging to show no synthetic field/value dependency
   - Messages persisted as normal conversational input (no field/value translation)

FRONTEND CHANGES:
4. components/alex/AlexChat.tsx
   - Already removed field dependency in P0 (no changes needed)
   - Only value parameter used, no field parameter
   - Natural language answers sent as-is

5. components/alex/AlexMessageList.tsx
   - Already removed field dependency in P0 (no changes needed)
   - NaturalOrchestrationAction renders without field requirement
   - Legacy AlexInteractiveQuestion updated to not use field

KEY ARCHITECTURAL CHANGES:
- AI orchestration is now authoritative over QuestionTracker
- QuestionTracker is advisory only (provides evidence, does not override)
- No generic duplicate fallback ("I think we've already discussed that")
- Natural user messages persisted naturally (no field/value requirement)
- AI receives natural-language context for decision-making
- Multiple requirements in one message respected
- Confirmation works naturally (proceed/yes/alright)
- Revision works naturally (plan changes not blocked by previous questions)
- P0 native orchestration preserved (orchestration events, action types)

AUTHORITY HIERARCHY:
1. User's actual message
2. Persisted AutomationPlan / orchestration state
3. AIOrchestrator / LLM decision (AUTHORITATIVE)
4. QuestionTracker (ADVISORY)
5. Legacy compatibility mechanisms (only where necessary)

VERIFICATION:
- AI remains final orchestration authority
- QuestionTracker is advisory (does not override AI decisions)
- No generic duplicate fallback controls normal conversation
- Natural user messages persisted naturally
- AI receives natural-language context
- Multiple requirements in one message respected
- Confirmation works naturally
- Revision works naturally
- P0 native orchestration preserved
- Artifact generation preserved

REMAINING P2 ISSUES (NOT FIXED IN P1):
- P2-A: Assumption handling (not enforced in plan generation)
- P2-B: AutomationSpec information loss (conversion is lossy)
- Additional: Conversation contamination (legacy path may still have issues)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>