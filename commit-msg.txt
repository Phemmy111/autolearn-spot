Remove legacy artifact_workflow UI contract, implement native orchestration actions

P0 IMPLEMENTATION SUMMARY:

Removed the legacy artifact_workflow protocol as the universal frontend interaction contract.
AI-driven orchestration actions now reach the frontend as native conversational AI actions,
preserving the LLM's chosen action type throughout the execution path.

BACKEND CHANGES:
1. lib/alex/orchestration/workflow-orchestrator.ts
   - Added action: AlexNextAction to WorkflowOrchestrationResponse
   - Modified handleOrchestrationResult() to preserve native AI action
   - All actions (clarify/recommend/brainstorm/plan/revise) now return with action preserved
   - generate/execute invoke artifact generation while preserving action

2. lib/alex/ai-engine.ts
   - Changed event type from artifact_workflow to orchestration
   - Added logging to show AI action type being emitted
   - Preserved backward compatibility in orchestrator metadata

3. app/api/alex/chat/route.ts
   - Added handler for orchestration event type
   - Removed universal artifact_workflow question serialization
   - Emits native orchestration event with action data to frontend
   - Preserved architecture/plan/artifacts handling for generate action

FRONTEND CHANGES:
4. components/alex/AlexChat.tsx
   - Added handler for orchestration event type
   - Stores orchestration data in orchestrationData field
   - Preserved legacy artifact_workflow handler for backward compatibility
   - Removed field dependency from question answer handling

5. components/alex/AlexMessageList.tsx
   - Added NativeOrchestrationAction component for action-based rendering
   - Renders clarify/recommend/brainstorm/plan/execute/revise with appropriate UI
   - Removed field dependency from question answer handling
   - Preserved legacy workflowData rendering for backward compatibility

KEY ARCHITECTURAL CHANGES:
- AI action identity preserved from LLM to frontend
- Frontend no longer depends on internal AutomationSpec field names
- Natural language answers (no field:value format required)
- Artifact generation machinery preserved
- Legacy artifact_workflow protocol remains only for backward compatibility

VERIFICATION:
- artifact_workflow is no longer the universal UI protocol
- AI action identity survives to the frontend (all 8 actions)
- No frontend field mapping required
- Natural language answers remain natural language
- Artifact generation still works (preserved machinery)
- AutomationPlan remains authoritative
- Existing functionality preserved

REMAINING PROBLEMS (NOT FIXED IN P0):
- P1: QuestionTracker aggressiveness (can override AI decisions)
- P1: Synthetic message persistence (conversation contamination)
- P2: Assumption handling (not enforced in plan generation)
- P2: AutomationSpec conversion (information loss)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>