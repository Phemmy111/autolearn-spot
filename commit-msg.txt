Make AI-driven orchestration the only production path, eliminate legacy routing bypass

P1.5 IMPLEMENTATION SUMMARY:

Eliminated the configuration-controlled routing bypass that could return ALEX to the
legacy wizard architecture. AI-driven orchestration is now the single authoritative
production orchestration path for normal ALEX conversations.

BACKEND CHANGES:
1. lib/alex/orchestrator.ts
   - Removed USE_AI_DRIVEN_ORCHESTRATION feature flag check from existing build routing (lines 164-208)
   - Removed USE_AI_DRIVEN_ORCHESTRATION feature flag check from new request routing (lines 227-281)
   - Removed legacy else branch that routed to WorkflowManagerV2
   - Added deprecation comments for the environment variable
   - AI-driven WorkflowOrchestrator is now the only path in both locations
   - Updated logging to indicate single production path
   - Updated intent detector logging to emphasize advisory metadata only

KEY ARCHITECTURAL CHANGES:
- Configuration-controlled routing bypass eliminated
- AI-driven orchestration structurally authoritative
- Legacy path removed from primary production routing
- Single orchestration brain for normal chat: ALEX AI ORCHESTRATOR
- USE_AI_DRIVEN_ORCHESTRATION environment variable deprecated (no longer controls routing)

FEATURE FLAG BEHAVIOR:
- USE_AI_DRIVEN_ORCHESTRATION=true → AI path (unchanged)
- USE_AI_DRIVEN_ORCHESTRATION=false → AI path (FIXED - no longer routes to legacy)
- Variable absent → AI path (unchanged)

LEGACY PATH STATUS:
- WorkflowManagerV2: NOT production-reachable for normal chat (preserved for artifact API and tests)
- IntelligenceAnalyzerV2: NOT production-reachable for normal chat (preserved for compatibility)
- Intent detector: ADVISORY METADATA ONLY (does not control routing)
- artifact_workflow: COMPATIBILITY-ONLY (primary contract is orchestration event)
- field:value parsing: NOT production-reachable for normal chat (legacy path only)

P0 REGRESSION CHECK:
- All 8 native actions preserved (respond/clarify/recommend/brainstorm/plan/generate/execute/revise)
- Native orchestration events preserved
- Frontend NativeOrchestrationAction preserved

P1 REGRESSION CHECK:
- QuestionTracker advisory only (preserved)
- No synthetic wizard answers (preserved)
- Natural-language persistence (preserved)
- Confirmation/revision behavior preserved (preserved)

REMAINING DETERMINISTIC GATES:
- Intent detector: ADVISORY METADATA ONLY (does not override AI)
- Existing build detection: ROUTING DECISION ONLY (AI still decides)
- QuestionTracker: ADVISORY ONLY (does not override AI)
- NO CRITICAL GATES remaining

ARCHITECTURE LEVEL:
- Before P1.5: LEVEL 2 (CONDITIONAL - could be LEVEL 3 or LEVEL 1 based on config)
- After P1.5: LEVEL 3 (Genuine conversational AI orchestration - structurally guaranteed)

CRITICAL PRODUCTION ISSUES RESOLVED:
- Feature flag dependency: RESOLVED (no longer controls routing)
- Legacy path still active: RESOLVED (removed from primary routing)
- Field value parsing in legacy path: RESOLVED (not reachable for normal chat)

VALIDATION:
- Static verification: PASS (correct architectural changes)
- Runtime verification: NOT EXECUTED (requires production deployment)
- Recommendation: Deploy with monitoring to confirm AI-driven path is active

DEPRECATION NOTES:
- USE_AI_DRIVEN_ORCHESTRATION: DEPRECATED (remove from production config after verification)
- Legacy orchestrator classes: PRESERVED for compatibility (consider removal after no dependencies confirmed)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>